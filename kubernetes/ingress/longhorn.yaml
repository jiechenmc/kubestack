apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: traefik.nx
spec:
  ingressClassName: traefik
  rules:
    - host: longhorn.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: longhorn-frontend
                port:
                  number: 80
